# Tiny Troll CTF — Hints

- Some flags are fake: they use the WDT{"..."} pattern.
- Check: view-source, stylesheets, scripts, the SVG, and copy/paste text (zero-width characters).
- Dev hint: open the console and try `decodeObf(obf)`.
